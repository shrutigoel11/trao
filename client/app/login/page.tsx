'use client'; import {FormEvent,useState} from 'react'; import {api} from '../../lib/api'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setErr('');setLoading(true);try{await api('/auth/login',{method:'POST',body:JSON.stringify({email:email.trim().toLowerCase(),password})});router.replace('/kits');router.refresh();}catch(e:any){setErr(e.message)}finally{setLoading(false)}}return <main className="max-w-md mx-auto px-5 py-16"><div className="card"><h1 className="text-3xl font-black">Login</h1><form onSubmit={submit} className="space-y-4 mt-6"><label className="block"><span className="sr-only">Email</span><input required autoComplete="email" className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block"><span className="sr-only">Password</span><input required autoComplete="current-password" className="input" type="password" minLength={8} placeholder="Password (8+ chars)" value={password} onChange={e=>setPassword(e.target.value)}/></label>{err&&<p role="alert" className="text-red-600">{err.replaceAll('_',' ')}</p>}<button disabled={loading} className="btn btn-primary w-full disabled:opacity-60">{loading?'Logging in…':'Login'}</button></form></div></main>}
