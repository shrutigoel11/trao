'use client'; import {useEffect,useMemo,useState} from 'react'; import {api} from '../../../lib/api'; import {useParams} from 'next/navigation';
export default function Practice(){const {id}=useParams();const [k,setK]=useState<any>(null),[i,setI]=useState(0),[show,setShow]=useState(false);useEffect(()=>{api('/kits/'+id).then(setK)},[id]);if(!k)return <main className="max-w-3xl mx-auto p-8">Loading…</main>;const cards=k.data.flashcards;const card=cards[i];async function confidence(n:number){const p={...(k.practice||{}),[card.id]:n};await api('/kits/'+id,{method:'PATCH',body:JSON.stringify({practice:p})});setK({...k,practice:p});setShow(false);setI((i+1)%cards.length)}const ordered=[...cards].sort((a:any,b:any)=>(k.practice?.[a.id]??0)-(k.practice?.[b.id]??0));const covered=cards.filter((c:any)=>k.practice?.[c.id]>0).length;if(!card)return <main className="p-8">No flashcards.</main>;return <main className="max-w-3xl mx-auto px-5 py-12"><p className="text-emerald-700 font-bold">PRACTICE MODE</p><h1 className="text-3xl font-black mt-2">Flashcards</h1><p className="muted mt-2">Covered {covered}/{cards.length}. Next sessions prioritise low-confidence cards.</p><div className="card mt-8 min-h-[330px]"><div className="text-xs muted">Card {i+1} of {cards.length}</div><h2 className="text-2xl font-black mt-8">{card.front}</h2>{show&&<div className="mt-8 border-t pt-6"><b>Answer</b><p className="mt-2 whitespace-pre-wrap">{card.back}</p></div>}{!show&&<button className="btn btn-primary mt-10" onClick={()=>setShow(true)}>Reveal answer</button>}{show&&<div className="mt-8"><p className="font-bold">How confident were you?</p><div className="flex gap-2 mt-3">{[1,2,3,4,5].map(n=><button key={n} className="btn btn-secondary" onClick={()=>confidence(n)}>{n}</button>)}</div></div>}</div><div className="card mt-5"><b>Weakest cards</b>{ordered.slice(0,5).map((c:any)=><p className="text-sm mt-2" key={c.id}>{c.front} · confidence {k.practice?.[c.id]??'not rated'}</p>)}</div></main>}
