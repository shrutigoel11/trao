'use client'; import {useState} from 'react';
export default function EditableText({value,onSave,multiline=false}:{value:string,onSave:(v:string)=>void,multiline?:boolean}){const [edit,setEdit]=useState(false),[v,setV]=useState(value);if(edit)return <div className="space-y-2"><textarea autoFocus className="input" value={v} onChange={e=>setV(e.target.value)}/><div><button className="btn btn-primary mr-2" onClick={()=>{onSave(v);setEdit(false)}}>Save</button><button className="btn btn-secondary" onClick={()=>setEdit(false)}>Cancel</button></div></div>;return <div onClick={()=>setEdit(true)} className="cursor-text">{value||<span className="muted">Click to edit</span>}</div>}
